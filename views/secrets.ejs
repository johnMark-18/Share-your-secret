<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8" />
    <title>Secrets</title>
    <link
      rel="stylesheet"
      href="https://use.fontawesome.com/releases/v5.6.3/css/all.css"
      integrity="sha384-UHRtZLI+pbxtHCWp1t77Bi1L4ZtiqrqD80Kn4Z8NTSRyMA2Fd33n5dQ8lWUE00s/"
      crossorigin="anonymous"
    />
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css"
      integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="css/styles.css" />
  </head>

  <body>
    <div class="container">
      <div class="jumbotron position-relative text-center">
        <div class="position-absolute" style="top: 10px; right: 20px">
          <a class="btn btn-dark btn-sm ml-2" href="/submit" role="button"
            >Submit a Secret</a
          >
          <a class="btn btn-light btn-sm" href="/profile" role="button"
            >Profile</a
          >
        </div>
        <i class="fas fa-key fa-6x"></i>
        <h1 class="display-3" style="font-size: 20px">
          You have discovered some anonymous secrets!
        </h1>

        <% if (secrets.length > 0) { %>
        <div class="secrets-container">
          <% secrets.forEach(secret => { %>
          <div class="card">
            <div class="card-body">
              <p class="card-text"><%= secret.content %></p>
            </div>
          </div>
          <% }); %>
        </div>
        <% } else { %>
        <p>No secrets yet!</p>
        <% } %>

        <hr />
      </div>
    </div>

    <script>
      window.addEventListener("pageshow", (event) => {
        var isNavigation =
          event.persisted ||
          (typeof window.performance != "undefined" &&
            window.performance.navigation.type === 2);
        if (isNavigation) {
          window.location.reload();
        }
      });
    </script>
  </body>
</html>
